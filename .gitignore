# 中间产物（可由 CHM 重新生成）
5z_src/
5z_web/
5z_build/.site-manifest.json
5z_build/watch.log

# 待更新的 CHM 输入目录与已处理存档
incoming/
5z_build/archive/

# 本地部署配置（含凭据，模板 deploy.config.example.json 入库）
5z_build/deploy.config.json

# 发布包
*.zip

# 系统文件
.DS_Store
Thumbs.db
desktop.ini
